Convert traitbank .zip file to turtle:

    python3 convert_to_ttl.py ~/a/eol/eol_website/felidae.zip felidae-ttl

Load the turtle into Jena and provide SPARQL service:

The Fuseki documentation is awful.  But there are some independent
how-to type articles.

Rricha Jalota says:

  - download Jena [3.10] and Fuseki
      (see https://jena.apache.org/download/ )
  - tar xzf apache-jena-3.10.0.tar.gz
  - tar xzf apache-jena-fuseki-3.10.0.tar.gz 
  - export JENA_HOME=~/Downloads/apache-jena-3.10.0
  - export FUSEKI_HOME=~/Downloads/apache-jena-fuseki-3.10.0
  - scripts are in bin/  e.g. $JENA_HOME/bin/sparql --version
  - (put all the lib/ jars on classpath)

Now start Fuseki:

    $FUSEKI_HOME/fuseki-server --file=example.ttl /ds

It doesn't take multiple --file parameters, so need to do something else.

    $FUSEKI_HOME/fuseki-server \
      --file=felidae-ttl/terms.ttl \
      --file=felidae-ttl/pages.ttl \
      --file=felidae-ttl/traits.ttl \
      --file=felidae-ttl/metadata.ttl \
      /ds

Fuseki seems to generate JSON sparql query result format by default.

Fuseki can generate JSON-LD for sparql CONSTRUCT commands.
